.playwright-mcp/
.claude/settings.local.json
.superpowers/
docs/superpowers/
node_modules/
dist/
*.tgz
.github/hooks/agent-viz.local.json
# Stray Playwright artifacts that escape .playwright-mcp/ when a tool call
# is invoked with a bare filename instead of a path-prefixed one.
/snapshot-*.yml
/page-*.yml

# Raw vitest output, when redirected to a file at the repo root instead of
# a scratch location.
/vitest-output.txt
